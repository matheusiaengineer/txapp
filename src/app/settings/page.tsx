"use client";import { useState, useEffect } from "react";import { motion } from "framer-motion";import { User, Bell, Shield, Palette, Globe, DollarSign, Info, Lock, Eye, Smartphone, Key, LogOut, ChevronRight, Moon, Sun, Monitor, Check, Fingerprint, CheckCircle, Loader2, AlertCircle, Award } from "lucide-react";import { useI18n } from "@/lib/i18n/provider";import { CURRENCIES } from "@/lib/payment/currencies";import { isBiometricSupported, registerBiometric } from "@/lib/auth/webauthn";type SettingsTab = "profile" | "notifications" | "privacy" | "security" | "appearance" | "language" | "currency" | "accessibility" | "about";export default function SettingsPage() {  const { t, locale, setLocale, locales } = useI18n();  const [tab, setTab] = useState<SettingsTab>("profile");  const [theme, setTheme] = useState<"dark" | "light" | "system">("dark");  const [biometricSupported, setBiometricSupported] = useState(false);  const [biometricRegistered, setBiometricRegistered] = useState(false);  const [biometricLoading, setBiometricLoading] = useState(false);  const [biometricMessage, setBiometricMessage] = useState<string | null>(null);  const [dataSaver, setDataSaver] = useState(false);  const [quietHours, setQuietHours] = useState(false);  const [deleteConfirmStep, setDeleteConfirmStep] = useState<0 | 1 | 2>(0);  const [showChangePassword, setShowChangePassword] = useState(false);  const [showSessions, setShowSessions] = useState(false);  const [fontSize, setFontSize] = useState<"normal" | "large" | "xlarge">("normal");  useEffect(() => {    isBiometricSupported().then(setBiometricSupported);    async function checkCredential() {      try {        const { hasBiometricCredential } = await import("@/lib/auth/webauthn");        const has = await hasBiometricCredential();        setBiometricRegistered(has);      } catch {}    }    checkCredential();  }, []);  async function handleBiometricToggle() {    if (biometricRegistered) return;    setBiometricLoading(true);    setBiometricMessage(null);    const res = await registerBiometric();    setBiometricLoading(false);    if (res.success) {      setBiometricRegistered(true);      setBiometricMessage("Biometria cadastrada com sucesso!");    } else {      setBiometricMessage(res.error || "Falha ao cadastrar biometria");    }    setTimeout(() => setBiometricMessage(null), 4000);  }  const tabs = [    { id: "profile" as SettingsTab, icon: <User className="w-5 h-5" />, label: "Perfil" },    { id: "notifications" as SettingsTab, icon: <Bell className="w-5 h-5" />, label: "Notificações" },    { id: "privacy" as SettingsTab, icon: <Eye className="w-5 h-5" />, label: "Privacidade" },    { id: "security" as SettingsTab, icon: <Shield className="w-5 h-5" />, label: "Segurança" },    { id: "appearance" as SettingsTab, icon: <Palette className="w-5 h-5" />, label: "Aparência" },    { id: "language" as SettingsTab, icon: <Globe className="w-5 h-5" />, label: "Idioma" },    { id: "currency" as SettingsTab, icon: <DollarSign className="w-5 h-5" />, label: "Moeda" },    { id: "accessibility" as SettingsTab, icon: <Smartphone className="w-5 h-5" />, label: "Acessibilidade" },    { id: "about" as SettingsTab, icon: <Info className="w-5 h-5" />, label: "Sobre" },  ];  return (    <div className="min-h-[100dvh] bg-background p-4 md:p-8 max-w-5xl mx-auto flex gap-6">      {/* Sidebar */}      <div className="hidden md:flex flex-col gap-1 w-52 shrink-0">        {tabs.map(t => (          <button key={t.id} onClick={() => setTab(t.id)}            className={`flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all ${              tab === t.id ? "bg-primary text-background" : "text-gray-400 hover:text-white hover:bg-card-bg"            }`}          >{t.icon} {t.label}</button>        ))}        <div className="mt-auto pt-4">          <button className="flex items-center gap-3 px-4 py-3 rounded-xl text-sm text-red-400 hover:bg-red-500/10 w-full transition-colors">            <LogOut className="w-5 h-5" /> Sair          </button>        </div>      </div>      {/* Content */}      <div className="flex-1">        {/* Mobile tabs */}        <div className="flex md:hidden gap-2 overflow-x-auto pb-4 mb-4">          {tabs.map(t => (            <button key={t.id} onClick={() => setTab(t.id)}              className={`flex items-center gap-2 px-4 py-2 rounded-full text-xs font-medium whitespace-nowrap transition-all ${                tab === t.id ? "bg-primary text-background" : "bg-card-bg text-gray-400"              }`}            >{t.icon} {t.label}</button>          ))}        </div>        <motion.div key={tab} initial={{ opacity: 0, x: 20 }} animate={{ opacity: 1, x: 0 }} className="space-y-4">          {/* PROFILE */}          {tab === "profile" && (            <div className="space-y-4">              <div className="glass-panel p-6 flex flex-col items-center text-center">                <div className="w-24 h-24 rounded-full bg-gradient-to-br from-primary/30 to-primary/10 flex items-center justify-center text-3xl font-bold text-primary mb-4 relative">                  TX                  <div className="absolute -bottom-1 -right-1 w-6 h-6 bg-yellow-500 rounded-full flex items-center justify-center border-2 border-background">                    <Award className="w-3 h-3 text-white" />                  </div>                </div>                <h2 className="text-xl font-bold">Thiago Xavier</h2>                <div className="flex items-center gap-2 mt-1">                  <p className="text-gray-400 text-sm">thiago@email.com</p>                  <span className="px-2 py-0.5 bg-yellow-400/15 text-yellow-400 text-[10px] font-semibold rounded-full">Verificado</span>                </div>                <button className="mt-4 text-primary text-sm hover:underline">Alterar foto</button>              </div>              <div className="glass-panel p-6 space-y-4">                {[                  { label: "Nome Completo", value: "Thiago Xavier" },                  { label: "Email", value: "thiago@email.com" },                  { label: "Telefone", value: "+55 (11) 99999-9999" },                  { label: "Data de Nascimento", value: "15/06/1990" },                  { label: "Bio", value: "Motorista parceiro TXD desde 2024" },                ].map(f => (                  <div key={f.label} className="flex items-center justify-between">                    <div><p className="text-sm text-gray-400">{f.label}</p><p className="font-medium">{f.value}</p></div>                    <button className="text-primary text-sm hover:underline">Editar</button>                  </div>                ))}              </div>            </div>          )}          {/* NOTIFICATIONS */}          {tab === "notifications" && (            <div className="glass-panel p-6 space-y-4">              <h2 className="text-lg font-bold mb-4">Preferências de Notificação</h2>              {[                { label: "Corridas", desc: "Novas solicitações, cancelamentos" },                { label: "Pagamentos", desc: "Recebimentos, saques, extratos" },                { label: "Promoções", desc: "Bônus, descontos, eventos" },                { label: "Segurança", desc: "Alertas de segurança, emergências" },                { label: "Sistema", desc: "Atualizações, manutenções" },                { label: "Mensagens", desc: "Novas mensagens no chat" },              ].map(n => (                <div key={n.label} className="flex items-center justify-between">                  <div><p className="font-medium text-sm">{n.label}</p><p className="text-xs text-gray-500">{n.desc}</p></div>                  <label className="relative inline-flex items-center cursor-pointer">                    <input type="checkbox" defaultChecked className="sr-only peer" />                    <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                  </label>                </div>              ))}              {/* Quiet hours */}              <div className="pt-2 border-t border-card-border">                <div className="flex items-center justify-between">                  <div><p className="font-medium text-sm">Horário silencioso</p><p className="text-xs text-gray-500">Não perturbe das 22h às 8h</p></div>                  <label className="relative inline-flex items-center cursor-pointer">                    <input type="checkbox" checked={quietHours} onChange={() => setQuietHours(!quietHours)} className="sr-only peer" />                    <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                  </label>                </div>                {quietHours && (                  <div className="mt-3 flex gap-2">                    <select className="bg-background border border-card-border rounded-lg px-3 py-1.5 text-xs text-white outline-none">                      <option>22:00</option><option>23:00</option><option>00:00</option>                    </select>                    <span className="text-gray-500 text-xs self-center">até</span>                    <select className="bg-background border border-card-border rounded-lg px-3 py-1.5 text-xs text-white outline-none">                      <option>06:00</option><option>07:00</option><option selected>08:00</option>                    </select>                  </div>                )}              </div>            </div>          )}          {/* PRIVACY */}          {tab === "privacy" && (            <div className="glass-panel p-6 space-y-4">              <h2 className="text-lg font-bold mb-4">Privacidade</h2>              {[                { label: "Mostrar status online", desc: "Outros motoristas veem quando você está online" },                { label: "Compartilhar histórico de corridas", desc: "Suas corridas aparecem no feed da comunidade" },                { label: "Mostrar ganhos no ranking", desc: "Seus ganhos aparecem no líderboard" },                { label: "Perfil público", desc: "Qualquer pessoa pode ver seu perfil" },              ].map(p => (                <div key={p.label} className="flex items-center justify-between">                  <div><p className="font-medium text-sm">{p.label}</p><p className="text-xs text-gray-500">{p.desc}</p></div>                  <label className="relative inline-flex items-center cursor-pointer">                    <input type="checkbox" defaultChecked={p.label === "Mostrar status online"} className="sr-only peer" />                    <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                  </label>                </div>              ))}              {/* Data saver */}              <div className="pt-2 border-t border-card-border">                <div className="flex items-center justify-between">                  <div><p className="font-medium text-sm">Modo economia de dados</p><p className="text-xs text-gray-500">Reduz qualidade de imagens e vídeos</p></div>                  <label className="relative inline-flex items-center cursor-pointer">                    <input type="checkbox" checked={dataSaver} onChange={() => setDataSaver(!dataSaver)} className="sr-only peer" />                    <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                  </label>                </div>              </div>            </div>          )}          {/* SECURITY */}          {tab === "security" && (            <div className="glass-panel p-6 space-y-4">              <h2 className="text-lg font-bold mb-4">Segurança</h2>              <div className="flex items-center justify-between p-4 bg-background rounded-xl">                <div className="flex items-center gap-3">                  <Key className="w-5 h-5 text-primary" />                  <div><p className="font-medium text-sm">Autenticação em 2 Etapas</p><p className="text-xs text-gray-500">Adicione uma camada extra de segurança</p></div>                </div>                <label className="relative inline-flex items-center cursor-pointer">                  <input type="checkbox" className="sr-only peer" />                  <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                </label>              </div>              <div className="flex items-center justify-between p-4 bg-background rounded-xl">                <div className="flex items-center gap-3">                  <Fingerprint className="w-5 h-5 text-primary" />                  <div>                    <p className="font-medium text-sm">Reconhecimento Biométrico</p>                    <p className="text-xs text-gray-500">                      {biometricSupported                        ? biometricRegistered ? "Biometria cadastrada" : "Face ID / Impressão digital"                        : "Não disponível neste dispositivo"}                    </p>                  </div>                </div>                {biometricSupported && (                  <button                    onClick={handleBiometricToggle}                    disabled={biometricLoading || biometricRegistered}                    className={`px-4 py-2 rounded-lg text-xs font-bold transition-all flex items-center gap-1 ${                      biometricRegistered                        ? "bg-primary/10 text-primary"                        : "bg-primary text-background hover:bg-primary-hover"                    } disabled:opacity-50`}                  >                    {biometricLoading ? (                      <Loader2 className="w-3 h-3 animate-spin" />                    ) : biometricRegistered ? (                      <><CheckCircle className="w-3 h-3" /> Ativo</>                    ) : (                      "Ativar"                    )}                  </button>                )}              </div>              {biometricMessage && (                <div className={`flex items-center gap-2 p-3 rounded-lg text-xs ${                  biometricMessage.includes("sucesso")                    ? "bg-primary/10 text-primary"                    : "bg-red-500/10 text-red-400"                }`}>                  {biometricMessage.includes("sucesso") ? <CheckCircle className="w-3 h-3" /> : <AlertCircle className="w-3 h-3" />}                  {biometricMessage}                </div>              )}              <button onClick={() => setShowChangePassword(!showChangePassword)}                className="w-full text-left p-4 bg-background rounded-xl flex items-center justify-between hover:bg-[#1a1a1a] transition-colors">                <div><p className="font-medium text-sm">Alterar Senha</p><p className="text-xs text-gray-500">Última alteração há 3 meses</p></div>                <ChevronRight className={`w-4 h-4 text-gray-500 transition-transform ${showChangePassword ? "rotate-90" : ""}`} />              </button>              {showChangePassword && (                <motion.div initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: "auto" }} className="overflow-hidden">                  <div className="bg-background rounded-xl p-4 space-y-3">                    <input type="password" placeholder="Senha atual" className="w-full bg-card-bg border border-card-border rounded-lg px-3 py-2.5 text-sm text-white outline-none focus:border-primary/30" />                    <input type="password" placeholder="Nova senha" className="w-full bg-card-bg border border-card-border rounded-lg px-3 py-2.5 text-sm text-white outline-none focus:border-primary/30" />                    <input type="password" placeholder="Confirmar nova senha" className="w-full bg-card-bg border border-card-border rounded-lg px-3 py-2.5 text-sm text-white outline-none focus:border-primary/30" />                    <button className="w-full bg-primary text-background text-sm font-bold py-2.5 rounded-lg">Salvar senha</button>                  </div>                </motion.div>              )}              <button onClick={() => setShowSessions(!showSessions)}                className="w-full text-left p-4 bg-background rounded-xl flex items-center justify-between hover:bg-[#1a1a1a] transition-colors">                <div><p className="font-medium text-sm">Sessões Ativas</p><p className="text-xs text-gray-500">2 dispositivos conectados</p></div>                <ChevronRight className={`w-4 h-4 text-gray-500 transition-transform ${showSessions ? "rotate-90" : ""}`} />              </button>              {showSessions && (                <motion.div initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: "auto" }} className="overflow-hidden">                  <div className="bg-background rounded-xl p-4 space-y-3">                    {[                      { device: "iPhone 15 Pro", location: "São Paulo, SP", time: "Agora", current: true },                      { device: "MacBook Air", location: "São Paulo, SP", time: "Há 2 horas", current: false },                    ].map((s, i) => (                      <div key={i} className="flex items-center justify-between">                        <div className="flex items-center gap-3">                          <Smartphone className="w-4 h-4 text-primary" />                          <div>                            <p className="text-sm font-medium">{s.device} {s.current && <span className="text-[10px] text-primary">(atual)</span>}</p>                            <p className="text-xs text-gray-500">{s.location} • {s.time}</p>                          </div>                        </div>                        {!s.current && <button className="text-[11px] text-red-400 hover:text-red-300">Sair</button>}                      </div>                    ))}                  </div>                </motion.div>              )}              {/* Delete account with 2-step confirmation */}              <div className="pt-4 border-t border-card-border">                {deleteConfirmStep === 0 ? (                  <button onClick={() => setDeleteConfirmStep(1)}                    className="w-full text-left p-4 bg-red-500/5 rounded-xl flex items-center justify-between hover:bg-red-500/10 transition-colors">                    <div><p className="font-medium text-sm text-red-400">Excluir conta</p><p className="text-xs text-gray-500">Esta ação não pode ser desfeita</p></div>                    <ChevronRight className="w-4 h-4 text-red-400" />                  </button>                ) : (                  <div className="bg-red-500/5 rounded-xl p-4 space-y-3">                    <p className="text-sm text-red-400 font-medium">                      {deleteConfirmStep === 1 ? "Tem certeza? Seus dados serão perdidos." : "Digite EXCLUIR para confirmar"}                    </p>                    {deleteConfirmStep === 1 ? (                      <div className="flex gap-2">                        <button onClick={() => setDeleteConfirmStep(0)} className="flex-1 py-2 bg-background text-gray-400 rounded-lg text-sm">Cancelar</button>                        <button onClick={() => setDeleteConfirmStep(2)} className="flex-1 py-2 bg-red-500 text-white rounded-lg text-sm font-bold">Sim, excluir</button>                      </div>                    ) : (                      <div className="flex gap-2">                        <input type="text" placeholder='Digite "EXCLUIR"' className="flex-1 bg-background border border-red-500/30 rounded-lg px-3 py-2 text-sm text-white outline-none" />                        <button className="py-2 px-4 bg-red-500 text-white rounded-lg text-sm font-bold disabled:opacity-50">Confirmar</button>                      </div>                    )}                  </div>                )}              </div>            </div>          )}          {/* APPEARANCE */}          {tab === "appearance" && (            <div className="glass-panel p-6 space-y-4">              <h2 className="text-lg font-bold mb-4">Aparência</h2>              <div className="grid grid-cols-3 gap-3">                {[                  { id: "dark" as const, icon: <Moon className="w-5 h-5" />, label: "Escuro" },                  { id: "light" as const, icon: <Sun className="w-5 h-5" />, label: "Claro" },                  { id: "system" as const, icon: <Monitor className="w-5 h-5" />, label: "Sistema" },                ].map(t => (                  <button key={t.id} onClick={() => setTheme(t.id)}                    className={`flex flex-col items-center gap-2 p-4 rounded-xl border transition-all ${                      theme === t.id ? "border-primary bg-primary/10 text-primary" : "border-card-border bg-background text-gray-400"                    }`}                  >{t.icon} <span className="text-sm">{t.label}</span></button>                ))}              </div>              <div className="pt-4">                <p className="text-sm text-gray-400 mb-3">Cor de destaque</p>                <div className="flex gap-3">                  {["#3ECB8E", "#8B5CF6", "#3B82F6", "#EC4899", "#F97316", "#EAB308", "#EF4444"].map(color => (                    <button key={color} onClick={() => {}}                      className="w-8 h-8 rounded-full border-2 border-transparent hover:scale-110 transition-transform"                      style={{ backgroundColor: color }}                    />                  ))}                </div>              </div>            </div>          )}          {/* LANGUAGE */}          {tab === "language" && (            <div className="glass-panel p-6">              <h2 className="text-lg font-bold mb-4">Idioma</h2>              <div className="space-y-1">                {locales.map(l => (                  <button key={l.code} onClick={() => setLocale(l.code)}                    className={`w-full flex items-center justify-between p-3 rounded-xl text-sm transition-all ${                      locale === l.code ? "bg-primary/10 text-primary" : "hover:bg-card-bg text-gray-300"                    }`}                  >                    <div className="flex items-center gap-3">                      <span className="text-lg">{l.native.includes("Português") ? "🇧🇷" : l.native.includes("English") ? "🇺🇸" : l.native.includes("Español") ? "🇪🇸" : l.native.includes("Français") ? "🇫🇷" : l.native.includes("Deutsch") ? "🇩🇪" : l.native.includes("Italiano") ? "🇮🇹" : l.native.includes("日本語") ? "🇯🇵" : l.native.includes("简体") ? "🇨🇳" : l.native.includes("हिन्दी") ? "🇮🇳" : l.native.includes("العربية") ? "🇸🇦" : l.native.includes("Русский") ? "🇷🇺" : l.native.includes("Nederlands") ? "🇳🇱" : l.native.includes("한국어") ? "🇰🇷" : l.native.includes("Türkçe") ? "🇹🇷" : "🌐"}</span>                      <span>{l.native}</span>                    </div>                    {locale === l.code && <Check className="w-4 h-4 text-primary" />}                  </button>                ))}              </div>            </div>          )}          {/* CURRENCY */}          {tab === "currency" && (            <div className="glass-panel p-6">              <h2 className="text-lg font-bold mb-4">Moeda</h2>              <div className="space-y-1">                {CURRENCIES.map(c => (                  <button key={c.code}                    className="w-full flex items-center justify-between p-3 rounded-xl text-sm hover:bg-card-bg text-gray-300 transition-all"                  >                    <div className="flex items-center gap-3">                      <span className="font-mono text-primary">{c.symbol}</span>                      <div><p className="font-medium">{c.code}</p><p className="text-xs text-gray-500">{c.name}</p></div>                    </div>                    <span className="text-xs text-gray-500">{c.isCrypto ? "Cryptocurrency" : "Fiat"}</span>                  </button>                ))}              </div>            </div>          )}          {/* ACCESSIBILITY */}          {tab === "accessibility" && (            <div className="glass-panel p-6 space-y-4">              <h2 className="text-lg font-bold mb-4">Acessibilidade</h2>              <div>                <p className="text-sm text-gray-400 mb-2">Tamanho da fonte</p>                <div className="flex gap-2">                  {[                    { id: "normal" as const, label: "Normal" },                    { id: "large" as const, label: "Grande" },                    { id: "xlarge" as const, label: "Extra" },                  ].map((opt) => (                    <button key={opt.id} onClick={() => setFontSize(opt.id)}                      className={`flex-1 py-2.5 rounded-xl text-sm font-medium transition ${                        fontSize === opt.id ? "bg-primary text-background" : "bg-background text-gray-400"                      }`}>                      {opt.label}                    </button>                  ))}                </div>              </div>              <div className="flex items-center justify-between pt-2">                <div><p className="font-medium text-sm">Reduzir movimentos</p><p className="text-xs text-gray-500">Desativa animações desnecessárias</p></div>                <label className="relative inline-flex items-center cursor-pointer">                  <input type="checkbox" className="sr-only peer" />                  <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                </label>              </div>              <div className="flex items-center justify-between">                <div><p className="font-medium text-sm">Alto contraste</p><p className="text-xs text-gray-500">Melhora a visibilidade dos elementos</p></div>                <label className="relative inline-flex items-center cursor-pointer">                  <input type="checkbox" className="sr-only peer" />                  <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                </label>              </div>              <div className="flex items-center justify-between">                <div><p className="font-medium text-sm">Leitor de tela</p><p className="text-xs text-gray-500">Otimizado para VoiceOver/TalkBack</p></div>                <label className="relative inline-flex items-center cursor-pointer">                  <input type="checkbox" defaultChecked className="sr-only peer" />                  <div className="w-11 h-6 bg-card-bg rounded-full peer peer-checked:bg-primary after:content-[''] after:absolute after:top-0.5 after:start-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full" />                </label>              </div>            </div>          )}          {/* ABOUT */}          {tab === "about" && (            <div className="space-y-4">              <div className="glass-panel p-6 text-center">                <div className="w-16 h-16 bg-primary rounded-2xl flex items-center justify-center mx-auto mb-4 text-2xl font-bold text-background">TX</div>                <h2 className="text-xl font-bold">TXD App</h2>                <p className="text-gray-400 text-sm">Versão 2.0.0 (Build 2024)</p>                <p className="text-xs text-gray-500 mt-1">Sistema Operacional Global de Mobilidade e Logística</p>              </div>              <div className="glass-panel p-6 space-y-3">                {[                  { label: "Termos de Uso", href: "#" },                  { label: "Política de Privacidade", href: "#" },                  { label: "Licenças de Código Aberto", href: "#" },                  { label: "Avaliar na App Store", href: "#" },                  { label: "Avaliar na Google Play", href: "#" },                ].map(l => (                  <button key={l.label} className="w-full text-left flex items-center justify-between text-sm text-gray-300 hover:text-white py-2 transition-colors">                    {l.label} <ChevronRight className="w-4 h-4 text-gray-500" />                  </button>                ))}              </div>            </div>          )}        </motion.div>      </div>    </div>  );}
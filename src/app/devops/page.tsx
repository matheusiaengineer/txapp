"use client";import { useState, useEffect } from "react";import { motion } from "framer-motion";import { Server, Activity, CheckCircle, AlertTriangle, XCircle, RefreshCw, Clock, Cpu, HardDrive, Wifi } from "lucide-react";import { serviceRegistry, type MicroService } from "@/lib/services/microservices";export default function DevOpsPage() {  const [services, setServices] = useState<MicroService[]>([]);  const [checking, setChecking] = useState(false);  const [status, setStatus] = useState({ total: 0, healthy: 0, degraded: 0, down: 0, uptime: 0 });  useEffect(() => {    setServices(serviceRegistry.getAll());    setStatus(serviceRegistry.getSystemStatus());  }, []);  async function checkAll() {    setChecking(true);    const results = await serviceRegistry.checkAllHealth();    setServices(serviceRegistry.getAll());    setStatus(serviceRegistry.getSystemStatus());    setTimeout(() => setChecking(false), 1000);  }  return (    <div className="min-h-[100dvh] bg-background p-4 md:p-8 max-w-6xl mx-auto">      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="flex items-center justify-between mb-8">        <div>          <h1 className="text-3xl font-bold flex items-center gap-3"><Server className="w-8 h-8 text-primary" /> System Health</h1>          <p className="text-gray-400 text-sm">Monitoramento de micro-serviços TXD</p>        </div>        <button onClick={checkAll} disabled={checking}          className="flex items-center gap-2 bg-primary hover:bg-primary-hover disabled:opacity-50 text-background px-5 py-2.5 rounded-xl font-bold text-sm transition-all"        ><RefreshCw className={`w-4 h-4 ${checking ? "animate-spin" : ""}`} /> {checking ? "Verificando..." : "Health Check"}</button>      </motion.div>      <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">        <div className="glass-panel p-4 text-center"><p className="text-2xl font-bold">{status.total}</p><p className="text-xs text-gray-500">Total</p></div>        <div className="glass-panel p-4 text-center border-green-500/30"><p className="text-2xl font-bold text-green-500">{status.healthy}</p><p className="text-xs text-gray-500">Healthy</p></div>        <div className="glass-panel p-4 text-center border-yellow-500/30"><p className="text-2xl font-bold text-yellow-500">{status.degraded}</p><p className="text-xs text-gray-500">Degraded</p></div>        <div className="glass-panel p-4 text-center border-red-500/30"><p className="text-2xl font-bold text-red-500">{status.down}</p><p className="text-xs text-gray-500">Down</p></div>      </div>      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">        {services.map((svc, i) => (          <motion.div key={svc.id} initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * 0.03 }}            className={`glass-panel p-5 border-l-4 ${              svc.status === "healthy" ? "border-l-green-500" : svc.status === "degraded" ? "border-l-yellow-500" : "border-l-red-500"            }`}          >            <div className="flex items-start justify-between mb-3">              <div>                <h3 className="font-bold text-sm">{svc.name}</h3>                <p className="text-xs text-gray-500">{svc.description}</p>              </div>              {svc.status === "healthy" ? <CheckCircle className="w-5 h-5 text-green-500" /> :               svc.status === "degraded" ? <AlertTriangle className="w-5 h-5 text-yellow-500" /> :               <XCircle className="w-5 h-5 text-red-500" />}            </div>            <div className="flex flex-wrap gap-x-4 gap-y-1 text-xs text-gray-500">              <span className="flex items-center gap-1"><Activity className="w-3 h-3" /> v{svc.version}</span>              <span className="flex items-center gap-1"><Clock className="w-3 h-3" /> {svc.uptime}% uptime</span>              <span className="flex items-center gap-1"><Cpu className="w-3 h-3" /> {svc.endpoints.length} endpoints</span>            </div>          </motion.div>        ))}      </div>    </div>  );}